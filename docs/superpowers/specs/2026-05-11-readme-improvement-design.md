# 技术方案：README.md 升级（GitHub 爆款风格）

**日期：** 2026-05-11
**状态：** 已批准
**主题：** 重构项目的 README，使其更具专业感、技术描述更详尽且视觉吸引力更强。

## 1. 概述
目标是将现有的 `README.md` 转换为一个高影响力的文档，突出项目的技术优势（YuNet AI、自然融合算法、`uv` 集成），同时保持现代、简洁的 GitHub 热门项目视觉风格。

## 2. 结构与内容

### 2.1 头部设计
- **标题：** `🛡️ Photo Privacy (人脸隐私保护工具)`
- **口号：** 一句话简介，强调分享照片时的安全与乐趣。
- **徽章 (Badges)：** 
    - `Python 3.10+`
    - `极速构建: uv`
    - `许可证: MIT`
    - `技术核心: OpenCV & YuNet`

### 2.2 核心特性 (为什么选择我们？)
- **AI 驱动检测：** 强调使用 YuNet，突出其高准确度和多尺度支持。
- **自然模糊融合：** 提及带羽化的高斯模糊算法（无生硬边缘）。
- **强大批处理：** 支持 ZIP 上传、多线程处理及智能路由。
- **隐私至上：** 100% 本地运行，数据不离身。
- **智能策略：** 解释单人 vs 多脸的差异化处理逻辑（头像替换 vs 局部模糊）。

### 2.3 技术亮点
- **推理引擎：** OpenCV `FaceDetectorYN`。
- **图像优化：** 自动 EXIF 旋转校正、缩略图预览性能优化。
- **环境管理：** 使用 `uv` 实现秒级环境同步。

### 2.4 安装与使用
- **现代路径：** 优先推荐 `uv run python app.py`。
- **传统路径：** 保留 `pip` 安装说明作为备选。
- **访问入口：** 明确默认端口 `5001`。

### 2.5 配置参数 (环境变量)
使用表格形式展示：
- `PHOTO_PRIVACY_DATA_DIR`
- `HOST`
- `PORT`
- `FLASK_DEBUG`

### 2.6 部署指南
- **Docker：** 精简的构建与运行命令。
- **生产环境：** 推荐使用 Gunicorn 及其参数配置（workers/threads）。

## 3. 实施细节
- 使用高质量 Emoji 区分章节。
- 确保所有专业术语（YuNet、高斯模糊、EXIF）使用准确。
- 增加“开发路线图”章节。

## 4. 自检清单
- **占位符：** 无 "TBD" 等待定项。
- **一致性：** 与 `app.py` 和 `face_processor.py` 的现有功能完全匹配。
- **范围：** 仅限文档改进，不涉及代码逻辑变更。
